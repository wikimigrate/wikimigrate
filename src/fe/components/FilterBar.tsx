import * as React from 'react'

const FilterBar = () => (
    <div style={{
        fontSize: "1.5em",
        padding: "0.5em"
    }}>
        Click to expand detailed filter
    </div>
)

export default FilterBar
