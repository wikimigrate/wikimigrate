import {
    MultiLangStringSet
} from "../../definitions"

const lang = "en"

const brandName: MultiLangStringSet = {
    en: "The Good Move",
    "zh-hans": "跑得快"
}

const version: string = "0.0.3"

const app = {
    lang,
    brandName,
    version,
}

export default app
